从HIS点击某个患者，助手同步切换到患者，展示数据，这个是怎么实现逻辑？双方数据写死吗？还是通过接口返回的？帮我分析一下。现在所有患者都是性别男，65岁


10:14



已处理 1m 2s



目前链路是这样的：
HIS/BS 侧上报文书上下文
前端通过 Tauri 命令读：
invoke<EmrContext | null>('get_latest_emr_context')
位置：src/services/emrContext/demoBsDetector.ts
轮询监听上下文变化
watchEmrContext(...)
位置：src/services/emrContext/watchEmrContext.ts
上下文变化后激活患者和文书
activateEmrContext(context, selectPatient, selectDoc)
位置：src/components/Layout/ExpandedEmrContextBridge.tsx
真正构造 Patient 的地方在：
buildPatientFromEmrContext(context)
位置：src/services/emrContext/activateEmrContext.ts
这里就是问题来源。现在代码是固定补的：
return {
  id: context.patientId,
  name: context.patientName,
  gender: '男',
  age: '65岁',
  bedNo: '1201',
  deptName: '心血管内科',
  admissionDate: '2026-06-01',
  admissionDays: 4,
  doctor: '林志远',
  diagnosis: '冠状动脉粥样硬化性心脏病',
};
所以结论是：患者姓名、住院号来自 HIS 上报；但性别、年龄、床号、科室、诊断等目前是前端写死补出来的。
这就是你看到所有患者都是“男 65岁”的原因。
另外还有一个手动读取入口：
getActivePatient()
位置：src/services/emsBridge.ts
它现在也返回固定的 SAMPLE_ACTIVE_PATIENT，不是接口真实返回。
要修的话有两种合理方案：
HIS 上报 EmrContext 时直接带完整患者信息：gender/age/bedNo/deptName/diagnosis/...，然后 buildPatientFromEmrContext 从 context 取值。
HIS 只上报 patientId，助手收到后调用后端接口，例如 GET /api/patients/{patientId}，拿完整患者详情再 selectPatient。
现在项目更像是方案 1 的半成品：已有 Tauri 上下文桥，但 EmrContext 类型只定义了 patientId/patientName/docCode/docName，患者详情还没接真实数据。